name: chronoshot
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0' # Flutter 3.19.6 ke liye sahi SDK

dependencies:
  flutter:
    sdk: flutter

  # Game Engine - Guaranteed Stable Version
  flame: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

